# Test profile placeholders — override with real DB when running integration tests locally.
eventpulse.security.remember-me-key=test-remember-me-key-not-for-production

spring.datasource.url=jdbc:postgresql://localhost:5432/eventpulse
spring.datasource.username=eventpulse
spring.datasource.password=eventpulse

spring.jpa.hibernate.ddl-auto=validate
spring.flyway.enabled=true
